// Zone 1 — Desktop UI Render Panels

//! Panel rendering functions — moved from mod.rs for partitioning
//! Each function is an `impl App` method.
//! Placeholder: actual implementations remain in mod.rs via `use panels::*;`

// For now, all render methods are still in mod.rs.
// This file exists to enable the module import.
