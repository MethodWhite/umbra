pub struct SnnClassifier;

impl SnnClassifier {
    pub fn new(_num_classes: usize, _input_size: usize) -> Self {
        Self
    }

    pub fn classify(&self, _input: Vec<Vec<bool>>) -> usize {
        0
    }
}
